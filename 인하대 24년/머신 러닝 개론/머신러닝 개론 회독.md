### Caushy Schwarz Inquality(코시-슈바 부등식)
For any $$x,y \in R^d \leq ||x||\cdot||y|| $$, with equality iff `x` and `y` are scalar multiples of one another.
두 임의 벡터의 inner prduct의 곱은 두 벡터의 norm 곱보다 작거나 같다.